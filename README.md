# Java Methods Practice – Application2

This repository contains **Application2.java**, a Java console program developed for an AUEB programming assignment.  
It includes implementations of common mathematical operations using **instance methods**, such as:

- Prime number check  
- Factorial calculation  
- Combinations (n choose k)  
- Counting digits of an integer  
- Solving a quadratic equation  
- Triangle area using Heron's formula  
- Reversing digits of an integer  
- Finding the minimum digit

---

## 📌 Features

### ✔ Prime Check  
Determines whether a number greater than 1 is prime.

### ✔ Factorial  
Computes `n!` using iterative multiplication.

### ✔ Combinations  
Uses the factorial method to compute:  
\[
C(n, k) = \frac{n!}{k!(n-k)!}
\]

### ✔ Digits of Integer  
Counts how many digits a positive integer has.

### ✔ Quadratic Equation  
Solves the equation  
\[
ax^2 + bx + c = 0
\]  
and prints the real solutions (if the discriminant ≥ 0).

### ✔ Triangle Area  
Implements Heron’s Formula:  
\[
A = \sqrt{s(s-a)(s-b)(s-c)}
\]  
with `s = (a+b+c)/2`.

### ✔ Reverse Digits  
Reverses the digits of an integer (e.g., 1234 → 4321).

### ✔ Minimum Digit  
Returns the smallest digit inside an integer (e.g., 5832 → 2).

---

## 🛠 How to Compile

```bash
javac Application2.java
```

## ▶ How to Run

```bash
java Application2
```

---

## 📂 File Structure

```
Application2.java
README.md
```

---

## 🧑‍🎓 Author

**Thomas Vasilas**

This project is for educational use.

